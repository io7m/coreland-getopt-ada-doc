(section
  (ref "getopt_argument_count")
  (title "Getopt.Argument_Count")
  (para function_desc "Return number of remaining arguments.")
  (subsection
    (title "Synopsis")
    (para-verbatim function_synopsis
"with Getopt;

function Argument_Count return Natural;
"))
  (subsection
    (title "Description")
    (para
      "The " (item ada_function "Argument_Count") " function returns the
      number of command line arguments remaining.")))
